using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Punto : MonoBehaviour {

	// Use this for initialization
	void Start () {
        Humano.puntos.Add(gameObject);
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
